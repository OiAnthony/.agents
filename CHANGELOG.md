# 全局 Skills 更新 CHANGELOG

## 2026-08-22

- **lark**（飞书技能）：
  - 新增「清空缓存」高风险操作红线：清缓存会清空整个环境的缓存，用户说「清一下缓存」只算确定了目标、不等于确认了这次清空——没拿到明确确认前只能出预览或停下询问，不能第一次调用就自动确认。
  - 多维表格（Base）新增表单分享管理：可启停分享、设置访问范围与匿名/登录要求；新增仪表盘分享管理：可启停、设置访问范围、是否保留返回源 Base 入口与智能分析开关；更新前先读当前配置，每次只改一个字段。
- **archify**（架构图技能）：
  - 新增桌面阅读器可读性保障：以 1440×900 桌面端阅读器为基准，自动计算节点文字投影后的字号，低于最小可读阈值（6px）即判为不可读，避免图在桌面端缩到看不清。
  - 新增真实浏览器回归测试（需本机 Chrome 与 ARCHIFY_CHROME 环境变量）：用示例架构图在桌面阅读器里实际渲染验证可读性；并新增旧版（v1）输入格式兼容性测试，防止历史格式的架构/数据流图在新版本渲染被改坏。

## 2026-08-20

- **lark**（飞书技能）：
  - 会议没有智能纪要但提供了妙记内容时，改为读取原始逐字稿并独立总结；缺少读取权限时，先说明原因，再经用户确认后发起权限申请。

## 2026-08-19

- **lark**（飞书技能）：
  - 共享底座（lark-shared）重构：原大而全的指南精简为「底座 + 按需索引」模式，身份与权限、JSON 输出契约、高风险操作审批、首次配置、更新通知五类规则拆成独立参考文档，底座只保留通用准则与安全红线。
  - 新增「强触发索引」机制：遇到身份/认证、判断命令成败、高风险写入、首次配置、更新提示五类场景时，必须先读对应参考文档再动手，同一文档只读一次，避免凭印象操作。
  - 补充两类新的命令输出提示（_notice）说明：本机技能与 CLI 版本不同步、以及使用旧命令时给出的兼容提示与替代命令。

## 2026-08-18

- **write**（写作润色技能）：
  - 新增「尊重作者原话」规则：当标记或改写的措辞其实是作者自己的表达时，先回源素材里检索确认，是原话就恢复原文，不要画蛇添足地改写。
  - 修正 AI 腔改用「减法」思路：只保留素材里已有的事实，不再额外添加新意象或修辞，避免改完反而更像机器腔。
  - 中文写作检查新增两条：段末「格言化收束」（把内容压成金句的修辞表演应删平或改自然）与「给读者贴标签」（替读者定义身份、预设心理的说法应避免）；规则清单补充需写明每条规则对应的可见效果。
- **archify**（架构图技能）：
  - 新增品牌标记能力：内置 107 个常用品牌标记（来源 Simple Icons 与 OpenAI 官方品牌指南），可为架构图、工作流、时序、数据流、生命周期图节点标注所属产品/服务，不影响节点原有语义。
  - 目录外的品牌采用显式捕获流程：先抓取官网图标生成带摘要校验的标记，作者确认后固定使用；网站内容变化或不可用时会明确失败，不会静默用错标记。

## 2026-08-17

- **check**（代码审查技能）：
  - 新增锁文件验证方法：遇到改动依赖锁文件的提交，不再靠肉眼读 diff 判断，而是用包管理器自身的更新命令重新生成锁文件并与对方结果逐字节比对——完全一致说明是真实工具生成的，有出入则可能是手工改动或解析不一致。
  - 自动化安全更新（如 Dependabot 类机器人提交）新增两项专项检查：整文件重新序列化比对（防止机器人改写清单时弄坏表情符号、中文等非 ASCII 字符或打乱键顺序），以及依赖可达性确认（确认该包确实被打进产物，避免把锁文件里无关条目当成真实漏洞上报）。
  - 新增版本固定（pin）处理指引：动任何 pin 之前先查它当初是为哪个 bug 加的，避免只升级其中一个而让配套 pin 失配，反而比不升级更糟。

## 2026-08-16

- **health**（工程健康度审计技能）：
  - 审计方法全面重构：废除按仓库规模自动分档的做法，改为「基于证据」审计——只依据风险、非显式约束、失败证据与验证覆盖四类证据下结论；文件数、贡献者数、大文件等仅作参考信号，不再自动触发深度审计或告警。
  - 新增「空壳验证器」检测：自动分析构建脚本、项目脚本与 CI 配置，识别只会打印、直接退出或只装依赖的假验证命令，避免把空壳脚本当成有效验证覆盖。
  - 报告输出新增敏感信息脱敏：命令清单中的 API 密钥、令牌与私有路径自动替换为占位符，防止审计报告泄露凭据。
  - 新增生成产物漂移检查：自动生成的内容与实际代码不一致时会给出告警。
  - 热点归属与重复修复分析改为风险导向：重复修复次数只是线索，需结合失败证据确认后才报告；不再仅凭文件大就要求热点归属说明。
  - 深度审计的触发条件简化，部分安全检查不再受项目规模档位限制。

## 2026-08-15

- **lark**（飞书技能）：
  - 多维表格（Base）能力大幅增强：新增 BaseApp 应用模式支持（页面与组件操作）；新增 9 份参考文档，覆盖字段类型与结构、公式字段、lookup 字段、仪表盘组件配置、工作流、角色权限配置与高级权限、记录查询与分析 SOP（本地/云端两条路径）。
  - 应用数据库新增 Base 数据同步能力：支持创建、查看、启用/停用、修改、删除同步任务，可做一次性批量同步或持续流式同步。
  - 细化文档评论路由：独立的评论操作走 Drive 子技能，随正文读取评论直接使用文档读取命令，减少来回切换。
  - 会议纪要链路（会议 → 纪要 → 读正文）明确了身份延续规则，全程沿用同一身份，避免权限错误。

## 2026-08-14

- **ui-ux-pro-max**（UI/UX 设计智能）：
  - 新增查询契约机制：根据任务类型选择最小搜索模式（整体设计系统 / 单一关注点 / 指定技术栈），一次查询围绕一个主导意图，结果先验证再使用，减少空结果和偏离主题的推荐。
  - 无障碍与文本排版查询体验改进：支持按可观测结果逐条搜索（如焦点不被遮挡、徽章文字换行、读屏播报等），先查语义结果再查对应技术栈实现。
  - 新增桌面端界面支持，覆盖 WPF、WinUI、JavaFX 等原生桌面技术栈。
  - 数据与质量改进：新增数据来源追踪、字体许可证、图标上游信息等数据文件，补充一批自动化测试（数据契约、样式分类、相关性评估、文本布局适配等）。
- **lark**（飞书技能）：
  - 新增多维表格 BaseApp（应用模式）操作指引：明确支持范围与限制（暂不支持复制应用、创建页面分组、从工作区移出资源等），遇到不支持的操作直接说明并停止，避免绕路探索。
  - 新增 BaseApp 组件数据配置参考文档，说明图表、列表、富文本等组件的配置规则。
- **archify**（架构图技能）：
  - 新增时序图列宽适配的自动化测试，保障生成图表时文字与列宽不溢出。

## 2026-08-13

- **lark**（飞书技能）：
  - 新增向聊天发送/上传文件的能力（此前只能发图片），发送图片也支持用机器人身份或普通账号两种方式。
  - 修正了部分场景的权限说明，多维表格数据分析、事件订阅、知识库等子技能的操作指引更准确。

## 2026-08-12

- **lark**（飞书技能）：
  - 新增审批流支持：可发起、撤销、审批、驳回、转交、加签、提醒等，覆盖完整审批流程。
  - 多维表格新增数据分析能力，支持云端分析或本地 Python 两种方式。
  - 电子表格新增样式设置，并附带一批便捷脚本（查子表、查工作簿、范围读写等），简化日常操作。
  - 幻灯片支持新增/删除/更新单页，并补充了命令行与底层 XML 操作参考。
  - 清理过期内容：移除文档字数统计、旧样式流程、旧 XML 演示稿等废弃文档，避免误导。
- **archify**（架构图技能）：
  - 新增可视化检查工具：生成架构图后自动检查文字是否截断、是否清晰可读。
  - 统一了架构、数据流、生命周期、时序、工作流五类图的文字适配逻辑，修复节点文字溢出或过小的问题。
