{
  "schema_version": 1,
  "diagram_type": "architecture",
  "meta": {
    "title": "Request processing architecture",
    "subtitle": "Authentication completes before business routing",
    "caption": "The gateway protects the service boundary.",
    "description": "A client sends a request to the gateway. The gateway authenticates the request and routes it to the application service, which reads the database.",
    "density": "editorial",
    "animation": "none"
  },
  "components": [
    { "id": "client", "label": "Client", "sublabel": "Browser", "row": 0, "col": 0 },
    { "id": "gateway", "label": "API Gateway", "sublabel": "Authentication", "row": 0, "col": 1, "focal": true, "detail": "Validates identity before routing." },
    { "id": "service", "label": "Application", "sublabel": "Business logic", "row": 0, "col": 2 },
    { "id": "database", "label": "Database", "sublabel": "Persistent state", "row": 1, "col": 2 }
  ],
  "connections": [
    { "from": "client", "to": "gateway", "label": "HTTPS", "role": "main" },
    { "from": "gateway", "to": "service", "label": "Authorized request", "role": "main" },
    { "from": "service", "to": "database", "label": "Read/write" }
  ],
  "boundaries": [
    { "label": "Trusted application boundary", "wraps": ["gateway", "service", "database"] }
  ]
}
